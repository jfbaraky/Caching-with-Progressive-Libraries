var _ = require('lodash'),
	gulp = require('gulp'),
	mocha = require('gulp-mocha'),
	defaultConfig = {
		entry: ''
	};

module.exports = function (config) {
	config = _.assign({}, defaultConfig, config);

	return function () {
		gulp.src(config.entry, { read: false })
		.pipe(mocha());
	};	
}